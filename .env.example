# 이 파일을 복사해서 ".env" 로 이름을 바꾼 뒤, 실제 키를 채워 넣으세요.
#   PowerShell:  Copy-Item .env.example .env
# .env 파일은 절대 외부에 공유/업로드하지 마세요. (.gitignore 에 이미 제외돼 있음)

# OpenAI API 키 (AI 분석 채팅 기능에 사용)
OPENAI_API_KEY=
# (선택) 사용할 모델. 기본값 gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini
