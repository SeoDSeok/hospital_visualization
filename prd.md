# 제품 요구사항 정의서 (PRD) - skill.md

## 1. 프로젝트 개요
* **프로젝트 이름**
  * 국문: 한국형 맥락인지 시군구 손상 정밀예방 플랫폼
  * 영문: National Precision Injury Prevention Platform (NPIPP)
* **한 줄 설명**
  * 질병관리청의 손상 자료와 소방청의 구급 데이터를 통합 분석하여 시군구 단위의 중증 손상 위험도를 공간 시각화하고, LLM 기반 대화형 인터페이스를 통해 맞춤형 예방 정책 수립을 지원하는 데이터 기반 분석 시스템
* **유형**
  * 데이터 분석 및 의사결정 지원용 BI(Business Intelligence) 웹 대시보드 애플리케이션
* **난이도**
  * **상 (High)**
  * *사유:* 대용량 보건의료 및 기상 데이터의 정제·캐싱 파이프라인 구축, GeoJSON 기반의 고도화된 Web-GIS(단계구분도) 구현, 사용자의 자연어 명령을 UI 제어로 변환하는 LLM Function Calling 연동 기술이 요구됨

---

## 2. 사용자 시나리오

### 🗺️ 시나리오 A: 시군구 지자체 및 보건소 정책 담당자 (핵심 사용자)
* **누가 (Who):** 지역 보건의료 정책을 기획하고 예산을 집행하는 지자체 공무원 및 보건소 담당자
* **언제 (When):** * 정부의 '제1차 손상관리종합계획'에 맞춰 우리 시군구만의 구체적인 실행 계획을 수립할 때
  * 폭염, 한파, 적설 등 계절성 기상위험 기후가 다가와 선제적인 취약계층 보호 조치가 필요할 때
* **왜 (Why):** 과거에는 전국 단위의 획일적인 지침만 있어서 지역별 현장에 진짜 필요한 사업이 무엇인지 알기 어려웠습니다. 분절되어 있던 기상, 인구, 손상 통계를 웹에서 통합적으로 보며 "우리 지역은 한파 시기에 65세 이상 고령자의 낙상 손상률이 급증하므로, 이 구역에 미끄럼 방지 패드 예산을 우선 배정해야 한다"와 같은 근거 기반의 지역 맞춤형 예방 정책(예방 패키지)을 수립하기 위해 사용합니다.

### 🚑 시나리오 B: 응급의료 자원 관제 및 병원 행정 담당자
* **누가 (Who):** 권역 응급의료센터 및 소방청 이송 체계를 모니터링하고 개선하는 책임자
* **언제 (When):** * 중증 손상 환자의 병원 간 전원(이송) 병목 현상이 발생하여 원인을 분석하고자 할 때
  * 지역 내 응급실 수용성 및 응급의료 접근성 격차를 진단할 때
* **왜 (Why):** 환자가 응급실에 온 뒤 다른 병원으로 가기까지(전원) 걸리는 시간과 중증화 부담을 줄여야 골든타임을 지킬 수 있습니다. 대시보드의 '전원 네트워크 샷(네트워크 중심성 분석)'과 '환자 상태 전이 Sankey 다이어그램'을 통해 어느 병원에서 어떤 이유(인력 부족, 병상 부족 등)로 지연이 발생하는지 취약 요인을 과학적으로 파악하고 인프라를 개선하기 위해 사용합니다.

### 💬 시나리오 C: 지역 주민 및 일반 시민
* **누가 (Who):** 본인이나 가족이 거주하는 지역의 손상 위험 환경에 관심이 있는 시민
* **언제 (When):** 우리 동네에서 자주 발생하는 사고 유형을 확인하고 일상 속 안전을 확보하고 싶을 때
* **왜 (Why):** 전문가용 대시보드나 복잡한 통계 표는 일반인이 이해하기 어렵습니다. 주민들은 대화형 AI 인터페이스(LLM) 창에 "우리 동네에서 어르신들이 가장 많이 다치는 장소가 어디야?"라고 자연어로 쉽게 질문하여, 거주 지역의 정밀 손상 위험 지수를 직관적으로 안내받고 생활 안전 주의 기준을 인지하기 위해 사용합니다.

---

## 3. 핵심 기능 목록

### 🔹 필수 기능 (Must-Have)
1. **글로벌 다중 조건 필터링 패널 (좌측 제어 패널)**
   * 분석 기간 설정 기능 (Date Range Picker)
   * 연령군 필터 기능 (전체 연령, 65세 이상 고령층 등 선택 제어)
   * 계절성 기상 조건 트리거 (전체 기상, 한파·적설, 폭염 특보 상황 연동)
   * 행정구역 및 지역 선택 드롭다운 (전국 단위 및 특정 시도/시군구 타깃팅)
   * 전역 상태 동기화 (필터 적용 버튼 클릭 시 지도, KPI, 차트가 실시간으로 일괄 업데이트되는 기능)
2. **Web-GIS 기반 시군구 단계구분도 (중앙 지도 패널)**
   * 대한민국 시군구 행정구역 경계(GeoJSON) 데이터 매핑 및 웹 화면 렌더링
   * 지역별 손상률 및 전원율 수치 레벨에 따른 동적 히트맵 색상 채우기(Choropleth)
   * 지도 인터랙션 기능 (마우스 호버 시 세부 지표 툴팁 팝업, 마우스 클릭 시 해당 지자체의 상세 프로파일 데이터 동기화)
3. **다중 지역 비교 분석 인터페이스 (Regional Comparison UI)**
   * 다중 선택 지원: 행정구역 드롭다운 또는 지도 클릭을 통해 2개 이상의 지역(예: 서울, 부산)을 동시에 선택하는 기능
   * 나란히 보기(Side-by-Side) 컴포넌트: 선택한 지역들의 주요 손상 지표, 취약 요인 Top 5, 중증도 피라미드 차트를 대조군 형태로 병렬 배치하여 가시적인 격차 분석 제공
4. **대화형 AI 분석 채팅 인터페이스 & UI 제어 (우측 패널)**
   * 자연어 질의응답을 위한 챗봇 UI 컴포넌트 구성 (텍스트 입력창, 전송 버튼, 메시지 타임라인 레이아웃)
   * LLM API 연동을 통한 통계 데이터 기반 자연어 가이드 문장 생성
   * 자연어 명령 기반 대시보드 제어 (Function Calling): 사용자의 채팅 입력 맥락을 인지(예: "한파 기간 전원율만 강조해서 보여줘")하여, 마우스 클릭 없이 화면의 필터와 지도를 자동 제어·동기화하는 인터페이스 기술
5. **상단 핵심 지표(KPI) 요약 카드**
   * 전체 손상률/전원율, 중증 손상률, 고령자 손상률, 응급대응 취약지역 수 요약 화면 표시
   * 각 지표 카드 내에 시계열 추세를 직관적으로 보여주는 미니 스파크라인(Sparkline) 꺾은선 차트 렌더링
6. **지역별 손상 프로파일 및 취약성 분석창 (하단 패널)**
   * 선택 지역의 손상/전원 취약요인 및 위험지수 분석 데이터 표출 (Top 5 동적 리스트업)
   * 손상 중증도 피라미드 퍼널 차트 (전체 손상 → 입원 → 중증손상 → 전원 → 사망 단계별 환자 규모 시각화)
7. **네트워크 거점 및 환자 전원(이송) 흐름 분석**
   * 시나리오 관점 선택 기능 (전체 보기, 허브 강조, 네트워크 거점 분석, 지역 집계, 전원 환자 중심)
   * 그래프 알고리즘 연산을 기반으로 한 병원/지역 간 이송 연결선 및 방향성 화살표(Arrowhead) 매핑
   * 목적지 병원(Outflow) 및 유입 병원(Inflow)의 전원 건수, 평균 전원 시간, 중증 비율 분석 데이터 테이블 연동
8. **통계 기반 랭킹 및 데이터 테이블 대시보드**
   * Hub Score, PageRank 중심성, 송수신 가중치 기반의 Top 20 병원/지역 랭킹 테이블(DataTable) 출력
   * 선택된 거점의 상위 중증 환자 대표 기록(Sample Rows) 그리드 출력 및 정렬 기능

### 🔸 선택 기능 (Nice-to-Have)
1. **환자 상태 전이 Sankey 다이어그램 전체화면 모달 (Modal)**
   * 환자의 내원 전 상태부터 1차 응급, 1차 입원, 2차 응급 및 입원 결과로 이어지는 세부 흐름 가시화 차트를 큰 화면으로 확대하여 볼 수 있는 팝업 모달창 연동
2. **분석 데이터 익스포트 (Export)**
   * 필터링된 시군구별 손상률/전원율 통계 및 랭킹 테이블 데이터를 사용자가 로컬 파일(CSV 또는 Excel)로 다운로드하는 기능

---

## 4. 기술 스택

### 4.1. 메인 개발 아키텍처 (기존 인프라 최적화 구조)
* **웹 프레임워크:** Python Dash (Flask 기반)
  * *UI 확장 라이브러리:* Dash Bootstrap Components (DBC) 또는 Dash Mantine Components (DMC)
* **데이터 시각화 엔진:** Plotly Graph Objects (`go.Choroplethmapbox`, `go.Sankey`, `go.Bar`, `go.Pie`)
* **데이터 가공 및 네트워크 분석:** Pandas, NumPy, NetworkX (중심성 및 가중치 연산 라이브러리)
* **대화형 AI 엔진:** OpenAI API SDK (Python) 또는 LangChain

### 4.2. 향후 확장 고려 아키텍처 (모던 풀스택 분리 구조)
* **프론트엔드 (Frontend):** React.js (또는 Next.js) + TypeScript / Tailwind CSS + Shadcn/ui
* **Web-GIS 및 차트:** Mapbox GL JS (또는 React-Map-GL) / Recharts (또는 Apache ECharts)
* **백엔드 API 서버 (Backend):** Python FastAPI (Pandas, NetworkX 기반 분석 로직을 API 엔드포인트화)

---

## 5. 화면 구성

전체 화면은 해상도에 유연하게 대응하는 **3단 분할 레이아웃(좌측 필터 / 중앙 메인 분석 / 우측 AI 채팅)**을 따릅니다.

+--------------------------------------------------------------------------------------------------+
|                                    [상단] 핵심 지표 (KPI) 요약 카드 영역                            |
|  (전체 손상률/전원율 + 스파크라인) | (중증 손상률 + 스파크라인) | (고령자 손상률) | (응급대응 취약지역수)  |
+-------------------+--------------------------------------------------------------+---------------+
|                   | [중앙] 메인 시각화 영역                                       |               |
|                   | - 시군구 단계구분도 (Choropleth Map)                         |               |
|                   | - 병원 및 지역 간 전원(이송) 네트워크 연결선                  |               |
| [좌측]            +--------------------------------------------------------------+ [우측]        |
| 글로벌            | [하단] 상세 프로파일 및 취약성 분석 영역                       | 통합 AI       |
| 다중 조건 필터     |                                                              | 분석 채팅창   |
| 패널              | * 일반 모드: 단일 지역 손상 피라미드 + 취약요인 Top 5         | - 대화 기록   |
|                   |                                                              | - 추천 질문칩 |
| (390px 고정폭)     | * 다중 비교 모드: [지역 A 프로파일]  vs  [지역 B 프로파일]     | - 입력창      |
|                   |                  (Side-by-Side 병렬 분할 뷰)                 | (460px 고정폭)|
+-------------------+--------------------------------------------------------------+---------------+


* **좌측 글로벌 필터 패널 (390px 고정):** 시나리오 선택, 레이어 토글, 기간, 권역, 환자 유형, 기상 트리거 등의 조작 컴포넌트 배치
* **중앙 메인 패널 (가변폭):** 상단 KPI 대시보드 카드(스파크라인 포함), 중단 GeoJSON 단계구분도 지도, 하단 상세 프로파일창(다중 지역 비교 선택 시 5:5 가로 병렬 그리드로 자동 전환)
* **우측 AI 분석 패널 (460px 고정):** 실시간 메시지 타임라인, 하단 추천 질문 칩셋, 텍스트 인풋 터미널 배치

---

## 6. 상세 기능 명세

### 6.1. 글로벌 다중 조건 필터링 및 전역 상태 관리
* **입력 트리거 (Input):** 좌측 패널 필터 요소들의 값 변경 이벤트 수신
* **백엔드 연산:** 원천 데이터프레임(`df`)에 대해 다중 조건 Boolean Indexing 쿼리 함수(`apply_filters`)를 가동하여 가공용 데이터프레임 서브셋(`df_f`) 추출
* **화면 출력 (Output):** 집계 메타데이터를 전역 상태 저장소(`dcc.Store`)에 동기화하고 연쇄 콜백 실행

### 6.2. Web-GIS 기반 시군구 단계구분도 및 전원 네트워크 시각화
* **단계구분도(Choropleth) 렌더링:** `df_f`에서 시군구별 손상률/전원율을 산출한 뒤 `go.Choroplethmapbox` 객체를 생성하여 GeoJSON 경계 내부에 등급별 색상 매핑
* **이송 네트워크 매핑:** `NetworkX` 기반의 Directed Graph 객체를 메모리에 빌드 후, 조건 범위 내 엣지만 `go.Scattermapbox(mode='lines')` 레이어로 매핑하고 방향성 화살표 오버레이
* **지도 인터랙션:** 마커나 구역 클릭 시 `clickData`에서 엔티티 ID를 추출하여 전역 선택 상태 API로 즉각 전달

### 6.3. 다중 지역 비교 분석 인터페이스 (Regional Comparison)
* **입력 트리거 (Input):** 드롭다운 다중 선택 또는 지도 상 복수 지역 연속 클릭 이벤트 감지
* **레이아웃 분할 로직:** 선택 배열의 길이가 2 이상일 때 하단 뷰 영역을 `display: flex; flexDirection: row` 구조의 5:5 병렬 분할 뷰로 자동 전환
* **데이터 테이블 및 차트 렌더링:** 분할 영역 각각에 독립적인 지역 서브셋 데이터를 바인딩하여 손상 중증도 피라미드 차트와 취약 요인을 대조 형태로 화면에 렌더링

### 6.4. 대화형 AI 분석 인터페이스 및 대시보드 제어 (Function Calling)
* **자연어 쿼리 송신:** 채팅창에 자연어 쿼리 입력 시 현재 대시보드 상태 객체 정보와 함께 OpenAI API 엔드포인트로 비동기 송신
* **의도 파악 및 함수 호출:** 시스템 프롬프트 상 정의된 JSON 스키마 제어 함수(`set_dashboard_filters`)를 매개로 사용자의 명령어 맥락 분석
* **UI 원격 제어 및 동기화:** 백엔드에서 분석된 JSON 제어 명령을 토대로 좌측 패널 필터 컴포넌트의 값을 강제 갱신(Force Update)하고 답변 텍스트를 타임라인에 출력

### 6.5. 상단 핵심 지표(KPI) 및 데이터 테이블 랭킹 그리드
* **상단 KPI 및 스파크라인:** `df_f` 기반 집계 수치를 표출함과 동시에 여백을 제거한 경량 라인 차트(`go.Scatter`)를 각 카드 배경에 스파크라인 형태로 렌더링
* **랭킹 및 데이터 그리드:** 선택 시나리오 관점에 매칭되는 네트워크 중심성 점수를 소팅하여 `dash_table.DataTable` 컴포넌트를 통해 상위 20개 행을 페이징 구조로 출력

---

## 7. 디자인 가이드

### 7.1. 색상 팔레트 (Color Palette)
* **Primary (핵심 분석 강조):** #10B981 (Emerald Green) - 정밀성, 안전, 긍정 지표 강조
* **Secondary (메인 프레임):** #020617 ~ #1E293B (Deep Navy / Slate Blue) - 의료 인프라의 신뢰감과 무게감
* **Danger/Warning (위험 임계치):** #EF4444 (Vibrant Red) / #F59E0B (Amber Orange) - 고위험군 및 취약 지역 매핑용
* **Neutral (배경 및 텍스트):** #F8FAFC (Off-White) / #94A3B8 (Slate Gray) - 저조도 환경의 텍스트 가독성 확보

### 7.2. 타이포그래피 (Typography)
* **H1 (플랫폼 타이틀):** 28px / Bold
* **H2 (패널/섹션 타이틀):** 20px / Bold
* **H3 (카드/차트 제목):** 16px / Medium
* **Body (일반 데이터/테이블):** 14px / Regular
* **Caption (차트 축 설명):** 12px / Regular

### 7.3. 컴포넌트 디자인 규격
* **대시보드 카드(Cards):** `Border Radius: 12px` 적용, 미세한 입체감(Shadow 효과) 부여를 통한 블록 독립성 확보
* **인터랙션 피드백:** 드롭다운/입력 필드 포커스 시 Primary 테두리 컬러 활성화, 지도/차트 호버 시 불투명도 조절 적용

---

## 8. 제약 사항

### 8.1. 개인정보 보호 및 데이터 비식별화 (Data Privacy)
* **민감 정보 노출 금지:** 웹 상에 노출되는 모든 보건의료 및 전원 이송 데이터는 시군구 또는 병원 단위로 완전히 집계(Aggregation)된 통계 데이터만을 다루어야 한다.
* **샘플 데이터 마스킹:** 랭킹 패널의 상위 환자 대표 기록 표출 시 환자 식별성 정보(성명, 생년월일 등)는 원천 배제하며 환자 일련번호(`EMS_SN`)는 무작위 가상 ID로 마스킹 처리한다.

### 8.2. 성능 및 자원 제약 (Performance & Resource)
* **대용량 데이터 연산 병목 방지:** Dash 프레임워크의 싱글 스레드 병목을 회피하기 위해 필터 조작 시 원천 데이터 전체를 연산하지 않고, 사전에 차원별 집계가 완료된 통계 테이블(Data Mart) 구조를 설계·조회하여 대시보드 로딩 속도를 2초 이내로 바인딩한다.
* **GIS 데이터 경량화:** 행정구역 경계 GeoJSON 데이터의 토폴로지를 단순화(Simplification)하여 지도 인터랙션(Zoom/Pan) 시 브라우저 단의 렌더링 지연(Lag)을 방지한다.

### 8.3. 대화형 AI 비용 및 토큰 제약 (AI & Token Management)
* **API 비용 최적화:** LLM 호출 시 전송되는 데이터 아키텍처의 컨텍스트 구조를 최소화하여 입력 토큰 비용을 최적화한다.
* **응답 지연 시간 관리:** AI 생성 문장의 응답 지연 시간 동안 대시보드가 멈춘 것처럼 보이지 않도록 백엔드 스트리밍(Streaming) 처리를 구현하거나 로딩 스피너(Spinner) UI를 채팅 영역 내에 배치해야 한다.

### 8.4. 시스템 이송성 제약 (Migration Readiness)
* **React/FastAPI 전환 대비:** 데이터 핸들링 백엔드 알고리즘 로직(Pandas 필터링, NetworkX 그래프 연산 함수군)은 D